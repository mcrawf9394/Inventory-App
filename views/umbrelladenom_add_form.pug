extends layout 

block content 
    h1= title 

    form(method="post")
        label(for="name") 
        input(name="name" type="text" required)
        label(for="summary")
        input(name="summary" type="text" required)
        button(type="submit") Submit
    if errors 
        ul 
            for error in errors
             li= error.msg